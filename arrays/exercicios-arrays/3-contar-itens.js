// Crie um array com nomes de cidades e exiba no console a quantidade total de itens usando a propriedade .length.

const cidades = ['são sebastião do passé', 'salvador', 'candeias', 'camaçari'];

// A propriedade length lê a lista e passa a quantidade total de itens
console.log(`A quantidade de cidades na lista é de: ${cidades.length}`);
