// Crie um array com 5 nomes. Use for para imprimir cada nome no console, um por linha.

let nomes = ['jonas', 'alberto', 'freitas', 'moura', 'bode'];

for (let i = 0; i < nomes.length; i++) {
    console.log(nomes[i]);
}
