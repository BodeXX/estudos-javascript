// Crie um array com nomes de usuários. Use forEach para imprimir "Olá, [nome]!" para cada um.

const nomes = ['user1', 'user2', 'user3', 'user4', 'user5'];

// Itera sobre a lista de usuários imprimindo uma saudação para cada um
nomes.forEach((nome) => {
    console.log(`Olá, ${nome}!`);
})
