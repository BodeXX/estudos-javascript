// Crie um objeto produto com preco e quantidade, e calcule o valor total da compra(preco * quantidade).

const carro = {
    nome: 'onix',
    preco: 100,
    quantidade: 3
}

console.log('O nome do produto é: ', carro.nome);

console.log('Valor total: ', carro.preco * carro.quantidade);
