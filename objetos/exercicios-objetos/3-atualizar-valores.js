// Modifique a propriedade "idade" com um novo valor.

const pessoa = {
    nome: 'Jonas',
    idade: 27,
    profissao: 'Programador'
}

// antes de modificar a propriedade
console.log(pessoa.idade);

pessoa.idade = 28;

// depois da modificação
console.log(pessoa.idade);
